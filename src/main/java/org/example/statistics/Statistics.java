package org.example.statistics;


import org.example.NoteStrusture.Note;
import org.example.NoteStrusture.Task;

public class Statistics {

    public static String getStatistics(Note note){
        /*for (Task task : note.getTasks()){
            if (task.isDone()){

            }
        }*/
        return "";
    }
}
